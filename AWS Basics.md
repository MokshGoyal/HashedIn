# AWS BASICS

---

***Question 1:
Your team needs a secure environment to test a new web application on AWS, with a focus on
persistent and reliable storage for application data. As the cloud engineer, you are asked to:
Task: Provision Private Linux EC2 with Persistent, Resizable Storage and NGINX***

***Subtask 1: Launch a Private EC2 Instance
• Launch an Ubuntu (latest LTS) t2.micro EC2 instance in an existing VPC private subnet (no
auto-assign public IP).
• Ensure the instance does not have a public IPv4 address.
• Enforce IMDSv2 only on the instance.***

// Did the subtask 1

---

***Subtask 2: Create, Encrypt, Attach, Format, and Persistently Mount EBS
• Create a gp3 EBS volume (10 GB) with encryption enabled and required tags.
• Attach it to the instance.
• Create one partition, format it ext4, and mount it at /data.
• Configure /etc/fstab using the UUID so it persists after reboot.***

// Did the subtask 2

---

***Subtask 3: Resize EBS and Expand Filesystem Online (No Downtime)
• Increase the EBS volume from 10 GB → 15 GB without stopping the instance.
• Expand the partition and then the filesystem so space is usable.
• Provide before/after outputs of lsblk and df -h.***

*Modify EBS Volume Size*

*From the  Amazon Web Services Console :*

1. *Go to EC2 → Volumes*
2. *Select the attached volume*
3. *Click Modify volume*
4. *Change size from 10 GiB → 15 GiB*
5. *Save changes (modification happens online)*

*Expand the partition using **sudo growpart /dev/xvdf 1** and the filesystem using **sudo resize2fs /dev/xvdf1** commands and then verify using the lsblk and df -h commands*

---

***Subtask 4: Deploy NGINX via User Data + Validate Persistence
• Use a user data script to install and start NGINX.***

***ANSWER** - Give the necessary script and start nginx using **systemctl restart nginx** and **systemctl enable nginx**.*

*To validate if the data is still mounted or persistant use **mount | grep /data** command*

***• Serve a sample page from /data (e.g., /data/site/index.html as the web root).***

*To validate from web locally, use **curl http://localhost** command and run on browser*

***• Reboot the instance and confirm:
 /data is still mounted.
 The site still serves correctly when accessed from within the VPC (e.g., via bastion
host or internal load balancer).***

Reconfirm using the same commands mentioned above

---

## *Question 4:*

***Scenario:
A data analytics company wants to automate the secure transfer and storage of daily log files
generated by their application servers. The logs must be collected from compute resources,
securely stored for future analysis, and access should be tightly controlled. The company also
wants to ensure that only authorized resources can write to the storage location.
Task: Private EC2 Log Collector with Least-Privilege S3 Upload Automation***

***• Subtask 1: Launch EC2 Log Collector
 Launch an Amazon EC2 instance (t2.micro, Ubuntu) to act as a log collector.***

1. *Launch an Ubuntu (latest LTS) EC2 instance*
2. *Instance type: t2.micro*
3. *Place the instance in a private subnet*
4. *Disable Auto-assign public IPv4*
5. *Security group:*
   * *Allow SSH only from bastion or internal CIDR*

---

***• Subtask 2: Create Least-Privilege IAM Role for S3 Log Writes
 Create an AWS Identity and Access Management (IAM) role that grants write access only to a specific Amazon Simple Storage Service (Amazon S3) bucket designated for log storage.***

Create a dedicated bucket for logs

Enable:

* **Block Public Access (all ON)**
* **Default encryption (SSE-S3)**

Then create a IAM policy that grants only write access to the bucket. Then create the IAM role using the following steps

1. IAM → Roles → Create role
2. Trusted entity: **Amazon EC2**
3. Attach the policy created above

---

***• Subtask 3: Attach Role and Validate Restricted Access
 Attach the IAM role to the EC2 instance.***

*** Verify the instance can upload log files to the designated S3 bucket, but cannot access other S3 buckets or unrelated AWS resources.***

*Attach IAM Role to EC2*

* *EC2 → Instance → Actions → Security → Modify IAM Role*

Create a log file and upload it to allowed s3 bucket

Verify by listing buckets using aws s3 ls command

---

***• Subtask 4: Automate Daily Log Upload via Cron
 Set up a scheduled task using cron on the EC2 instance to upload a sample log file
to the S3 bucket daily.***

Create a upload script using sudo nano /usr/local/bin/upload_logs.sh command.

Then edit the crontab using crontab -e command

And add the entry to update daily on midnight using 0 0 * * * /usr/local/bin/upload_logs.sh >> /var/log/log_upload.log 2>&1 command

---

***• Subtask 5: Demonstrate End-to-End Operation
 Demonstrate the scheduled upload runs as expected.
 Confirm the log file appears in the target S3 bucket.***

Manually Trigger the script using /usr/local/bin/upload_logs.sh and verify by using cat command on the log file

---


***Question 5:
Scenario:
A tech company wants to modernize its web application deployment process using containers and AWS managed services. They need to ensure that their custom web server image is securely stored, easily deployed, highly available, and monitored for health and performance.
Task: Deploy Scalable Apache Container on AWS ECS with ALB and Monitoring***


•** *Subtask 1: Build and Publish Custom Apache Docker Image to ECR
 Create a custom Docker image running Apache HTTP Server.
 Push the image to Amazon Elastic Container Registry (Amazon ECR).***

**
*• Subtask 2: Deploy Apache Service on ECS with Auto Scaling
 Set up an Amazon Elastic Container Service (Amazon ECS) cluster.
 Deploy the Apache container as an ECS service with 0.25 vCPU, 0.5GB Memory
 Enable service auto scaling based on CPU utilization.***

**
*• Subtask 3: Configure Application Load Balancer and Health Checks
 Configure an Application Load Balancer (ALB) to distribute incoming HTTP traffic to
the ECS service.
 Set up container health checks (via the ALB target group) to ensure only healthy
tasks receive traffic.***

**
*• Subtask 4: Enable Observability (Logs + Alarms)
 Enable Amazon CloudWatch Logs for the ECS service to capture container logs.
 Set up a CloudWatch alarm for high CPU usage.***

***• Subtask 5: Validate End-to-End Deployment
 Access the Apache welcome page via the Load Balancer DNS name.
 Show CloudWatch metrics for the ECS service to demonstrate scaling/health visibility***


1. **Create Dockerfile** : Create a file named `Dockerfile` that uses `httpd:2.4` as the base image and copies a custom `index.html` file to the server's root directory.
2. **Provision ECR Repo** : Run the `aws ecr create-repository` command to create a private repository named `custom-apache-repo` for storing your image.
3. **Build and Push Image** : Authenticate Docker to ECR, build the image locally with a specific tag, and push it to the `custom-apache-repo` URI in ECR.
4. **Create Load Balancer** : Use the `aws elbv2 create-load-balancer` command to provision an internet-facing Application Load Balancer across two public subnets.
5. **Configure Target Group** : Create a Target Group with `IP` target type using `aws elbv2 create-target-group` and set the health check path to `/` on port 80.
6. **Setup Listener** : Create an HTTP listener on port 80 for the Load Balancer that forwards all incoming traffic to the Target Group created in the previous step.
7. **Register Task Definition** : Create a task definition JSON file specifying the ECR image URI, 0.25 vCPU, 0.5GB memory, and the CloudWatch log group configuration, then register it with ECS.
8. **Deploy ECS Service** : Create an ECS Service using the Fargate launch type, setting the desired count to 2, and linking it to the Load Balancer's Target Group.
9. **Enable Auto Scaling** : Register the ECS service as a scalable target and apply a Target Tracking scaling policy to maintain average CPU utilization at 70%.
10. **Verify Deployment** : Retrieve the Load Balancer's DNS name to access the website, and use the CloudWatch console to verify that logs are appearing and CPU metrics are being tracked.
